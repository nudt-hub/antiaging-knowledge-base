name: Monitor ClockBase Agent

on:
  schedule:
    - cron: '0 2 * * 1'  # 每周一凌晨 2:00 UTC（北京时间 10:00）
  workflow_dispatch:  # 允许手动触发

jobs:
  monitor-clockbase:
    runs-on: ubuntu-latest
    
    steps:
      - uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
      
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install requests
      
      - name: Create data directory
        run: mkdir -p data
      
      - name: Run monitoring script
        id: monitor
        run: |
          python scripts/sync-clockbase.py
          echo "monitor_completed=true" >> $GITHUB_OUTPUT
      
      - name: Check for changes
        id: git-check
        run: |
          git diff --quiet content/clockbase.md || echo "has_changes=true" >> $GITHUB_OUTPUT
      
      - name: Commit and push changes
        if: steps.git-check.outputs.has_changes == 'true'
        run: |
          git config --local user.email "action@github.com"
          git config --local user.name "GitHub Action"
          git add content/clockbase.md data/
          git commit -m "docs: 自动监控 ClockBase Agent $(date +%Y-%m-%d)"
          git push
      
      - name: Create issue if monitoring fails
        if: failure()
        uses: peter-evans/create-issue-from-file@v4
        with:
          title: "⚠️ ClockBase Agent 监控失败"
          content-filepath: data/monitor-error.md
          labels: automated, monitoring, clockbase
      
      - name: Upload monitoring report
        uses: actions/upload-artifact@v4
        with:
          name: monitoring-report
          path: data/
